import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Verificador1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
